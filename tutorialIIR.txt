especificação vem em freq analogica
preciso transformar em digital 

SE FOR passa-faixa ou rejeita-faixa
   se for geometricamente simetrico ok
   senao passo pelo algoritmo pra corrigir(passos i,ii,iii,iv do slide)
  
aí faz a normalização (ve qual o filtro e olha a tabela)

acha o `a`
pra achar `e` bota na fórmula
pra achar `n` que é a ordem bota na fórmula

polos

função de transferencia

transformação(bilinear)
